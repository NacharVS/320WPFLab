<Page x:Class="BaseEmptyApp.CharacterPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:BaseEmptyApp"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="CharacterPage" Background="Violet">
    <Page.Resources>
        <Style TargetType="TextBox">
            <Setter Property="FontSize" Value="20" />
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="FontSize" Value="20" />
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="10*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="10*"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Column="1" Grid.Row="1" DataContext="{Binding Character}">
            <TextBlock x:Name="TbLabel" TextAlignment="Center"/>
            <TextBlock Text="Strength"/>
            <TextBox x:Name="TbStrength" Text="{Binding Strength}"/>
            <TextBlock Text="Dexterity"/>
            <TextBox x:Name="TbDexterity"  Text="{Binding Dexterity}"/>
            <TextBlock Text="Intelligence"/>
            <TextBox x:Name="TbIntelligence" Text="{Binding Intelligence}"/>
            <TextBlock Text="Constitution"/>
            <TextBox x:Name="TbConstitution" Text="{Binding Constitution}"/>
            <TextBlock Text="Health"/>
            <TextBox x:Name="TbHealth" IsEnabled="False"/>
            <TextBlock Text="Mana"/>
            <TextBox x:Name="TbMana" IsEnabled="False"/>
            <Button x:Name="BtnChange" Content="Change" FontSize="20" Margin="10" Click="BtnChange_Click"/>
        </StackPanel>
        
    </Grid>
    
</Page>
